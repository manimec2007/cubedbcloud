cubes:
  - name: my_date_dimension
    sql_table: pharmsight_csl.MY_DATE_DIMENSION


    dimensions:
      - name: my_date
        sql: MY_DATE
        type: time
        primary_key: true

      - name: month_name
        sql: MONTH_NAME
        type: string

    joins:
      - name: shipment
        sql: "{CUBE}.MY_DATE = {shipment}.SHIP_DATE"
        relationship: one_to_many

      dimensions:
      - name: patient_id
        sql: MDM_PAT_ID
        type: string

      - name: month_name
        sql: MONTH_NAME
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

